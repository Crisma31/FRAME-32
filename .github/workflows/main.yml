name: Windows - LiteManager

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    timeout-minutes: 60  # Ajustado a 60 minutos (ajusta según tus necesidades)

    steps:
      - name: Checkout code 
        uses: actions/checkout@v3 

      - name: Downloading & Installing Essentials
        run: |
          Invoke-Webrequest -Uri "https://gitlab.com/chamod2/lm_win-10_github_rdp/-/raw/main/Downloads.bat" -OutFile "Downloads.bat" -ErrorAction Stop
          cmd /c Downloads.bat

      - name: Connect to LiteManager
        run: echo "Conectando a LiteManager..." # Reemplazar con el comando real para conectar a LiteManager

      - name: Time Counter
        run: cmd /c loop.bat
